let x = 10;
let y = 15;
let z = 25;

const subtract = (x, y) => {
    return x - y;
}

const divide = (x, y) => {
    return x / y;
}

const sum = (x, y, z) => {
    return x + y + z;
}

const multiplyParams = (x, y) => {
    return x * y;
}